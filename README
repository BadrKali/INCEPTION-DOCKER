
# Inception Project

## Overview

The Inception project is a part of the 42 School curriculum, focusing on creating a web application using Docker. This project demonstrates the ability to manage containers and orchestrate services with Docker Compose.

## Setup

To get started, ensure you have Docker and Docker Compose installed on your machine. Use the provided `Makefile` to manage Docker Compose commands efficiently.

## Usage

### Commands

- **Start the Project:**

  ```bash
  make up
